
/**
 * We used void to when we are annotating the return type of
 * functions and it's a way of saying this function doesn't
 * return any value
 */
function printTwice(msg: string): void {
  console.log(msg);
}
